### 一个还凑合的单链式结构的栈

由于我们要在此章中大致地介绍Rust的方方面面，并且，在构建项目的时候也会故意加大难度来加深你对语言的理解，所以这一章节将会是这些章节中最长的那一个。

要实现的第一个链表我们把它放在 ```src/first.rs``` 这个文件中。此外，对于我们的库项目来说，我们需要告诉Rust ```first.rs``` 是我们的库要使用到的文件，要做到这一点，只需要在 ```src/lib.rs``` (这个文件是我们使用cargo new --lib命令时Cargo自动帮我们创建的) 中添加如下代码即可：

```rust
// 在lib.rs中添加
pub mod first;
```

